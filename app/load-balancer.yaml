apiVersion: v1
kind: Service
metadata:
  name: load-balancer
  labels:
    app: dockerized-react
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    nodePort: 31001
  selector:
    app: dockerized-react